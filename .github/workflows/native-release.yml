name: native-release

on:
  push:
    tags:
      - "openmodels-v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing openmodels-v* tag pointing at the selected commit
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: native-release-${{ inputs.release_tag || github.ref }}
  cancel-in-progress: false

env:
  CARGO_INCREMENTAL: "0"
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"

jobs:
  tag-check:
    name: Validate release tag and commit
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    outputs:
      release_tag: ${{ steps.validate.outputs.release_tag }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Validate selected tag
        id: validate
        shell: bash
        env:
          RELEASE_TAG: ${{ github.event_name == 'push' && github.ref_name || inputs.release_tag }}
        run: |
          set -euo pipefail
          [[ "$RELEASE_TAG" =~ ^openmodels-v[0-9][0-9A-Za-z._-]*$ ]]
          tag_commit="$(git rev-parse "refs/tags/${RELEASE_TAG}^{commit}")"
          [[ "$tag_commit" == "$GITHUB_SHA" ]]
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

  fork-contracts:
    name: Fork contracts (${{ matrix.platform }})
    needs: tag-check
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            os: ubuntu-22.04
          - platform: windows
            os: windows-2022
    runs-on: ${{ matrix.os }}
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "22.13.1"
          cache: npm
          cache-dependency-path: sudhir_codex/cursor_worker/package-lock.json
      - uses: ./.github/actions/setup-ci
        if: matrix.platform == 'linux'
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        if: matrix.platform == 'linux'
        with:
          components: rustfmt
      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        if: matrix.platform == 'linux'
        with:
          version: "0.11.3"

      - name: Install Linux contract dependencies
        if: matrix.platform == 'linux'
        shell: bash
        run: python -m pip install --disable-pip-version-check --editable ./sudhir_codex ruff==0.15.13

      - name: Install Windows contract dependencies
        if: matrix.platform == 'windows'
        shell: pwsh
        run: python -m pip install --disable-pip-version-check --editable ./sudhir_codex ruff==0.15.13

      - name: Verify and run the compile-free prebuild gate
        shell: bash
        env:
          CONTRACT_PLATFORM: ${{ matrix.platform }}
        run: |
          set -euo pipefail
          python scripts/tests/verify_sudhir_fork_contracts.py lint \
            --platform "$CONTRACT_PLATFORM"
          python scripts/tests/run_sudhir_fork_contracts.py \
            --platform "$CONTRACT_PLATFORM" \
            --phase prebuild \
            --evidence "fork-contract-tests-${CONTRACT_PLATFORM}.json"
          python scripts/tests/staged_artifact_acceptance.py \
            --phase ci \
            --contracts scripts/tests/sudhir_fork_contracts.toml \
            --platform "$CONTRACT_PLATFORM" \
            --source-commit "$GITHUB_SHA" \
            --test-evidence "fork-contract-tests-${CONTRACT_PLATFORM}.json" \
            --output "fork-contract-evidence-${CONTRACT_PLATFORM}.json"

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: fork-contract-evidence-${{ matrix.platform }}
          path: fork-contract-evidence-${{ matrix.platform }}.json
          if-no-files-found: error
          retention-days: 7

  windows-dependency-preflight:
    name: Windows Python and rusty_v8 dependency preflight
    needs: fork-contracts
    runs-on: windows-2022
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"
      - name: Verify exact Windows rusty_v8 assets and checksums
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: x86_64-pc-windows-msvc
      - name: Confirm non-empty Windows rusty_v8 outputs
        shell: bash
        run: |
          set -euo pipefail
          test -s "$RUSTY_V8_ARCHIVE"
          test -s "$RUSTY_V8_SRC_BINDING_PATH"

  macos:
    name: Apple Silicon macOS bundle
    needs:
      - tag-check
      - fork-contracts
      - windows-dependency-preflight
    runs-on: macos-15
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: aarch64-apple-darwin
      - name: Configure rusty_v8 artifacts
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: aarch64-apple-darwin

      - name: Build required macOS binaries
        shell: bash
        run: |
          set -euo pipefail
          cargo build \
            --locked \
            --target aarch64-apple-darwin \
            --release \
            --bin codex \
            --bin codex-code-mode-host \
            --bin codex-responses-api-proxy

      - name: Strip, ad-hoc sign, and smoke-test
        shell: bash
        run: |
          set -euo pipefail
          stage="${RUNNER_TEMP}/openmodels-macos"
          release="target/aarch64-apple-darwin/release"
          export CODEX_HOME="${RUNNER_TEMP}/openmodels-codex-home"
          mkdir -p "$CODEX_HOME"
          rm -rf "$stage"
          mkdir -p "$stage"
          for binary in codex codex-code-mode-host codex-responses-api-proxy; do
            cp "$release/$binary" "$stage/$binary"
            strip -S -x "$stage/$binary"
            codesign --force --sign - "$stage/$binary"
            codesign --verify --verbose=2 "$stage/$binary"
            file "$stage/$binary" | grep -q 'arm64'
            lipo "$stage/$binary" -verify_arch arm64
          done
          "$stage/codex" --version
          "$stage/codex" --help >/dev/null
          "$stage/codex" app-server --help >/dev/null
          "$stage/codex" mcp-server --help >/dev/null

          archive="${GITHUB_WORKSPACE}/codex-with-openmodels-aarch64-apple-darwin.tar.gz"
          tar -C "$stage" -czf "$archive" \
            codex \
            codex-code-mode-host \
            codex-responses-api-proxy

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: macos-release-archive
          path: codex-with-openmodels-aarch64-apple-darwin.tar.gz
          if-no-files-found: error
          retention-days: 3

  ubuntu:
    name: Ubuntu 22.04-compatible x64 MUSL bundle
    needs:
      - tag-check
      - fork-contracts
      - windows-dependency-preflight
    # Match OpenAI's MUSL 1.2.4 release toolchain. The resulting static binary
    # does not depend on the build host and is tested later on Ubuntu 22.04.
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: x86_64-unknown-linux-musl

      - name: Use a hermetic Cargo home
        shell: bash
        run: |
          set -euo pipefail
          cargo_home="${GITHUB_WORKSPACE}/.cargo-home"
          mkdir -p "${cargo_home}/bin"
          echo "CARGO_HOME=${cargo_home}" >> "$GITHUB_ENV"
          echo "${cargo_home}/bin" >> "$GITHUB_PATH"
          : > "${cargo_home}/config.toml"

      - name: Install Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.14.0
          use-cache: false

      - name: Install MUSL build tools
        env:
          TARGET: x86_64-unknown-linux-musl
        run: bash "${GITHUB_WORKSPACE}/.github/scripts/install-musl-build-tools.sh"

      - name: Install Linux crash diagnostics
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            --no-install-recommends gdb strace

      - name: Disable aws-lc jitter entropy
        shell: bash
        run: |
          set -euo pipefail
          echo "AWS_LC_SYS_NO_JITTER_ENTROPY=1" >> "$GITHUB_ENV"
          echo "AWS_LC_SYS_NO_JITTER_ENTROPY_x86_64_unknown_linux_musl=1" >> "$GITHUB_ENV"

      - name: Configure rusty_v8 artifacts
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: x86_64-unknown-linux-musl

      - name: Build, strip, and hash bundled bwrap
        shell: bash
        run: |
          set -euo pipefail
          target="x86_64-unknown-linux-musl"
          cargo build --locked --target "$target" --release --bin bwrap
          bwrap="target/${target}/release/bwrap"
          strip --strip-debug --strip-unneeded "$bwrap"
          digest="$(sha256sum "$bwrap" | awk '{print $1}')"
          echo "CODEX_BWRAP_SHA256=$digest" >> "$GITHUB_ENV"
          echo "Bundled bwrap sha256:$digest"

      - name: Build required Linux binaries
        shell: bash
        run: |
          set -euo pipefail
          cargo build \
            --locked \
            --target x86_64-unknown-linux-musl \
            --release \
            --bin codex \
            --bin codex-code-mode-host \
            --bin codex-responses-api-proxy

      - name: Strip, stage, and smoke-test
        shell: bash
        run: |
          set -euo pipefail
          # setup-python is needed only by the rusty_v8 verifier. Do not expose
          # its host-library override to the native MUSL runtime smoke.
          unset LD_LIBRARY_PATH
          stage="${RUNNER_TEMP}/openmodels-linux"
          release="target/x86_64-unknown-linux-musl/release"
          export CODEX_HOME="${RUNNER_TEMP}/openmodels-codex-home"
          mkdir -p "$CODEX_HOME"
          rm -rf "$stage"
          mkdir -p "$stage/codex-resources"

          run_codex_smoke() {
            label="$1"
            binary="$2"
            echo "Testing ${label} Linux binary: ${binary}"
            file "$binary"
            readelf -h "$binary"
            readelf -l "$binary"
            readelf -d "$binary" || true
            ldd "$binary" || true
            if readelf -l "$binary" | grep -q 'INTERP'; then
              echo "${label} Linux binary is not self-contained" >&2
              return 1
            fi

            set +e
            "$binary" --version
            status=$?
            set -e
            if [[ "$status" -ne 0 ]]; then
              echo "${label} Linux binary exited with status ${status}" >&2
              gdb -q -batch \
                -ex "set pagination off" \
                -ex run \
                -ex "thread apply all bt" \
                --args "$binary" --version || true
              trace="${RUNNER_TEMP}/codex-${label}.strace"
              strace -f -o "$trace" "$binary" --version || true
              tail -n 120 "$trace" || true
              return "$status"
            fi
          }

          run_codex_smoke unstripped "$release/codex"
          for binary in codex codex-code-mode-host codex-responses-api-proxy; do
            cp "$release/$binary" "$stage/$binary"
            strip --strip-debug --strip-unneeded "$stage/$binary"
            file "$stage/$binary" | grep -Eq 'x86-64|x86_64'
          done
          cp "$release/bwrap" "$stage/codex-resources/bwrap"
          file "$stage/codex-resources/bwrap" | grep -Eq 'x86-64|x86_64'
          run_codex_smoke stripped "$stage/codex"
          "$stage/codex" --help >/dev/null
          "$stage/codex" app-server --help >/dev/null
          "$stage/codex" mcp-server --help >/dev/null

          archive="${GITHUB_WORKSPACE}/codex-with-openmodels-x86_64-unknown-linux-musl.tar.gz"
          tar -C "$stage" -czf "$archive" \
            codex \
            codex-code-mode-host \
            codex-responses-api-proxy \
            codex-resources/bwrap

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: ubuntu-release-archive
          path: codex-with-openmodels-x86_64-unknown-linux-musl.tar.gz
          if-no-files-found: error
          retention-days: 3

  windows-build:
    name: Windows x64 build
    needs:
      - tag-check
      - fork-contracts
      - windows-dependency-preflight
    runs-on: windows-2022
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    env:
      LIBSQLITE3_FLAGS: SQLITE_DISABLE_INTRINSIC
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Configure short Windows build paths
        shell: pwsh
        run: |
          $BuildRoot = Join-Path $env:RUNNER_TEMP "codex-build"
          $CargoTarget = Join-Path $BuildRoot "target"
          $Temp = Join-Path $BuildRoot "tmp"
          New-Item -ItemType Directory -Path $CargoTarget, $Temp -Force | Out-Null
          "CARGO_TARGET_DIR=$CargoTarget" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "TEMP=$Temp" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "TMP=$Temp" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          git config --global core.longpaths true
      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: x86_64-pc-windows-msvc
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"
      - name: Configure rusty_v8 artifacts
        uses: ./.github/actions/setup-rusty-v8
        with:
          target: x86_64-pc-windows-msvc
      - name: Configure the MSVC and Windows SDK environment
        uses: ./.github/actions/setup-msvc-env
        with:
          target: x86_64-pc-windows-msvc

      - name: Build all required Windows binaries once
        shell: bash
        run: |
          set -euo pipefail
          cargo build \
            --locked \
            --target x86_64-pc-windows-msvc \
            --release \
            --bin codex \
            --bin codex-app-server \
            --bin codex-code-mode-host \
            --bin codex-command-runner \
            --bin codex-responses-api-proxy \
            --bin codex-windows-sandbox-setup

      - name: Stage and inspect exact Windows binaries
        shell: pwsh
        run: |
          $Target = "x86_64-pc-windows-msvc"
          $Release = Join-Path $env:CARGO_TARGET_DIR "$Target\release"
          $Stage = Join-Path $env:RUNNER_TEMP "openmodels-windows"
          New-Item -ItemType Directory -Path $Stage -Force | Out-Null
          $Binaries = @(
            "codex",
            "codex-app-server",
            "codex-code-mode-host",
            "codex-command-runner",
            "codex-responses-api-proxy",
            "codex-windows-sandbox-setup"
          )
          foreach ($Binary in $Binaries) {
            $Executable = "$Binary.exe"
            $Source = Join-Path $Release $Executable
            if (-not (Test-Path -LiteralPath $Source -PathType Leaf)) {
              throw "Missing runtime binary $Source"
            }
            $Headers = & dumpbin.exe /headers $Source
            if (($Headers -join "`n") -notmatch "8664 machine \(x64\)") {
              throw "$Executable is not an x64 PE binary"
            }
            Copy-Item -LiteralPath $Source -Destination (Join-Path $Stage $Executable)
          }

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: windows-release-binaries
          path: ${{ runner.temp }}/openmodels-windows/*.exe
          if-no-files-found: error
          retention-days: 3

  windows:
    name: Windows x64 bundle
    needs:
      - tag-check
      - fork-contracts
      - windows-build
    runs-on: windows-2022
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.13"

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "22.13.1"
          cache: npm
          cache-dependency-path: sudhir_codex/cursor_worker/package-lock.json

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-release-binaries
          path: ${{ runner.temp }}/openmodels-windows

      - name: Smoke-test and package native Windows binaries
        shell: pwsh
        run: |
          $Stage = Join-Path $env:RUNNER_TEMP "openmodels-windows"
          $Binaries = @(
            "codex.exe",
            "codex-app-server.exe",
            "codex-code-mode-host.exe",
            "codex-command-runner.exe",
            "codex-responses-api-proxy.exe",
            "codex-windows-sandbox-setup.exe"
          ) | Sort-Object
          $Actual = @(
            Get-ChildItem -LiteralPath $Stage -File |
              ForEach-Object { $_.Name } |
              Sort-Object
          )
          $Difference = Compare-Object `
            -ReferenceObject $Binaries `
            -DifferenceObject $Actual
          if ($null -ne $Difference) {
            $Difference | Format-Table | Out-String | Write-Host
            throw "Assembled Windows binaries do not match the release layout"
          }

          $env:HOME = Join-Path $env:RUNNER_TEMP "openmodels-user-home"
          $env:CODEX_HOME = Join-Path $env:RUNNER_TEMP "openmodels-codex-home"
          New-Item -ItemType Directory -Path $env:HOME -Force | Out-Null
          New-Item -ItemType Directory -Path $env:CODEX_HOME -Force | Out-Null
          & (Join-Path $Stage "codex.exe") --version
          if ($LASTEXITCODE -ne 0) { throw "codex.exe --version failed" }
          & (Join-Path $Stage "codex.exe") --help | Out-Null
          if ($LASTEXITCODE -ne 0) { throw "codex.exe --help failed" }
          & (Join-Path $Stage "codex.exe") app-server --help | Out-Null
          if ($LASTEXITCODE -ne 0) { throw "codex.exe app-server --help failed" }
          & (Join-Path $Stage "codex.exe") mcp-server --help | Out-Null
          if ($LASTEXITCODE -ne 0) { throw "codex.exe mcp-server --help failed" }

          $Archive = Join-Path $env:GITHUB_WORKSPACE "codex-with-openmodels-x86_64-pc-windows-msvc.zip"
          Compress-Archive -Path (Join-Path $Stage "*.exe") -DestinationPath $Archive
          $Checksums = Join-Path $env:RUNNER_TEMP "SHA256SUMS"
          $Hash = (Get-FileHash -LiteralPath $Archive -Algorithm SHA256).Hash.ToLowerInvariant()
          "$Hash  $([System.IO.Path]::GetFileName($Archive))" |
            Set-Content -LiteralPath $Checksums -Encoding Ascii
          & "$env:GITHUB_WORKSPACE\scripts\sudhir-codex.ps1" `
            -Archive $Archive `
            -Checksums $Checksums `
            -ValidateArchiveOnly

      - name: Smoke-test Windows installation and gateway lifecycle
        shell: pwsh
        run: |
          $SmokeRoot = Join-Path $env:RUNNER_TEMP "native-gateway-lifecycle"
          $State = Join-Path $SmokeRoot "state"
          $PiAgentDir = Join-Path $SmokeRoot "pi-agent"
          $Archive = Join-Path $env:GITHUB_WORKSPACE "codex-with-openmodels-x86_64-pc-windows-msvc.zip"
          $Checksums = Join-Path $env:RUNNER_TEMP "SHA256SUMS"

          & "$env:GITHUB_WORKSPACE\scripts\sudhir-codex.ps1" `
            -Archive $Archive `
            -Checksums $Checksums `
            -Root $env:GITHUB_WORKSPACE `
            -State $State `
            -PiAgentDir $PiAgentDir
          if ($LASTEXITCODE -ne 0) { throw "Native Windows installation failed" }

          $GatewayPython = Join-Path $env:GITHUB_WORKSPACE ".venv\Scripts\python.exe"
          $env:SUDHIR_CODEX_ROOT = $env:GITHUB_WORKSPACE
          $env:SUDHIR_CODEX_STATE = $State
          $env:SUDHIR_CODEX_PI_AGENT_DIR = $PiAgentDir
          $Started = $false
          try {
            & $GatewayPython -m sudhir_codex_gateway.management start
            if ($LASTEXITCODE -ne 0) { throw "Gateway start smoke failed" }
            $Started = $true
            $StatusJson = (& $GatewayPython -m sudhir_codex_gateway.management status --json) -join "`n"
            if ($LASTEXITCODE -ne 0) { throw "Gateway status smoke failed" }
            $Status = $StatusJson | ConvertFrom-Json
            if (-not $Status.running) { throw "Gateway status did not report running" }
          }
          finally {
            if ($Started) {
              & $GatewayPython -m sudhir_codex_gateway.management stop
              if ($LASTEXITCODE -ne 0) { throw "Gateway stop smoke failed" }
            }
          }
          if (Test-Path -LiteralPath (Join-Path $State "gateway\gateway.pid")) {
            throw "Gateway PID file remained after stop"
          }

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: windows-release-archive
          path: codex-with-openmodels-x86_64-pc-windows-msvc.zip
          if-no-files-found: error
          retention-days: 3

  release:
    name: Assemble checksums and draft pre-release
    needs:
      - tag-check
      - fork-contracts
      - macos
      - ubuntu
      - windows
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: "*-release-archive"
          merge-multiple: true
          path: release-assets

      - name: Verify archives and create SHA256SUMS
        shell: bash
        run: |
          set -euo pipefail
          cd release-assets
          expected=(
            codex-with-openmodels-aarch64-apple-darwin.tar.gz
            codex-with-openmodels-x86_64-pc-windows-msvc.zip
            codex-with-openmodels-x86_64-unknown-linux-musl.tar.gz
          )
          for archive in "${expected[@]}"; do
            test -f "$archive"
          done
          [[ "$(find . -maxdepth 1 -type f | wc -l | tr -d ' ')" == "3" ]]
          tar -tzf codex-with-openmodels-aarch64-apple-darwin.tar.gz >/dev/null
          tar -tzf codex-with-openmodels-x86_64-unknown-linux-musl.tar.gz >/dev/null
          unzip -t codex-with-openmodels-x86_64-pc-windows-msvc.zip >/dev/null
          sha256sum "${expected[@]}" > SHA256SUMS
          sha256sum -c SHA256SUMS

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: complete-release-candidate
          path: release-assets/*
          if-no-files-found: error
          retention-days: 7

      - name: Create or refresh the draft pre-release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.tag-check.outputs.release_tag }}
        run: |
          set -euo pipefail
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            draft="$(
              gh api "repos/${GITHUB_REPOSITORY}/releases/tags/${RELEASE_TAG}" \
                --jq '.draft'
            )"
            [[ "$draft" == "true" ]] || {
              echo "Refusing to replace assets on a non-draft release" >&2
              exit 1
            }
            gh release upload "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --clobber \
              release-assets/*
          else
            gh release create "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --verify-tag \
              --target "$GITHUB_SHA" \
              --draft \
              --prerelease \
              --title "$RELEASE_TAG" \
              --notes "Owner-validation candidate built once for macOS arm64, Ubuntu 22.04 x64, and Windows x64. Verify SHA256SUMS before testing. The Windows archive is unsigned." \
              release-assets/*
          fi
